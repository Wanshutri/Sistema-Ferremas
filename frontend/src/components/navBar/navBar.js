import './navBar.css' // Podemos importar estilos independientes para cada componentes (solo se aplciaran a este y no globalmente)

const NavBar = () => {
    return ( // SOLO UN NAVBAR DE PRUEBA SACADO DE BOOTSTRAP
        0
    )
}

export default NavBar;